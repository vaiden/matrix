image: pyfunceble/pyfunceble:latest

# only:
#   - schedules

before_script:
  - echo "Before script section"
  - echo "For example you might run an update here or install a build dependency"
  - echo "Or perhaps you might print out some debugging details"

after_script:
  - echo "After script section"
  - echo "For example you might do some cleanup here"

build1:
  stage: build
  script:
    - echo "Do your build here"


test1:
  stage: test
  script:
    - echo "Do a test 1 here"
    - echo "For example run a test suite"
    # split over multiply lines
    # https://docs.gitlab.com/ee/ci/yaml/script.html#split-long-commands
    - >
      docker run -it -v ${PWD}/inside:/home/pyfunceble
      -v /var/run/mysqld/mysqld.sock:/var/run/mysqld/mysqld.sock
      pyfunceble/pyfunceble -f source/adware/domains.list

test2:
  stage: test
  script:
    - echo "Do another parallel test here"
    - echo "For example run a lint test"
    - >
      docker run -it -v ${PWD}/inside:/home/pyfunceble
      -v /var/run/mysqld/mysqld.sock:/var/run/mysqld/mysqld.sock
      pyfunceble/pyfunceble -f source/adware/wildcard.list

deploy1:
  stage: deploy
  script:
    - echo "Do your deploy here"
